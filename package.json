{
  "name": "wechat260531",
  "version": "0.1.0",
  "description": "微信公众号编辑器 AI 助手：Chrome 扩展 + 本地 Fastify 服务。",
  "license": "MIT",
  "private": false,
  "scripts": {
    "install:all": "npm --prefix chrome-extension install && npm --prefix local-server install",
    "build": "npm --prefix local-server run build && npm --prefix chrome-extension run build",
    "start:server": "npm --prefix local-server run start",
    "smoke": "powershell -ExecutionPolicy Bypass -File scripts/smoke-test.ps1"
  }
}
